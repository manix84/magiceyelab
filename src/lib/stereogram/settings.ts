import type { StereogramSettings } from "../../types/stereogram";

export const defaultStereogramSettings: StereogramSettings = {
  width: 1200,
  height: 800,
  depthStrength: 45,
  repeatWidth: 120,
  invertDepth: false,
};
